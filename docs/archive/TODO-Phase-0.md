# Phase 0 — Foundations

### Completed

- [x] **Crash reporter wired (Sentry or Crashlytics).** Disabled in debug, opt-in in release.
- [x] **CI baseline.** GitHub Actions: clang-format check, ktlint, build core (Linux), build Android (Linux + NDK), run core unit tests. Target wall-clock < 8 min.
- [x] **Single-clip preview playback loop.** Extend the verified MediaCodec first-frame path into native-fps playback for the bundled H.264 sample, still without timeline editing. Reference `docs/architecture.md` §9 decode/playback strategy.
- [x] **Decide JSON dependency strategy.** We will vendor `nlohmann::json` to handle the growing project model (arrays for tracks, clips) in Phase 1. → write [ADR-0009](../adr/0009-json-dependency.md) outcome.
- [x] **Decide & document GPU backend choice for Android v1.** Vulkan vs OpenGL ES 3.2 fallback strategy. → write [ADR-0004](../adr/0004-gpu-backend-android.md) outcome. We chose Vulkan with a GLES 3.2 fallback for older devices and the emulator.
- [x] **Verify `VulkanBackend` smoke test.** The Android emulator (sdk_gphone16k_x86_64) fails with `VK_ERROR_INITIALIZATION_FAILED`. We will implement GLES 3.2 fallback to unblock development.

- [x] **2026-04-26 — Added Phase 0 core application/FFI/test scaffold.** Added `ProjectService`, FFI implementations for lifecycle/version/status/string/project functions, and a no-dependency desktop test harness matching [`docs/ffi.md`](../ffi.md) and [ADR-0006](../adr/0006-clean-hexagonal-architecture.md).
- [x] **2026-04-26 — Verified CMake build and CTest for `core/` locally.** Used Android SDK CMake/Ninja to configure and build the desktop core with `VX_BUILD_TESTS=ON`, `VX_WERROR=ON`, and sanitizers off. Fixed exported C ABI symbol visibility and top-level CTest registration, then confirmed all five native tests pass via `ctest --test-dir /tmp/video-editor-cmake-build --output-on-failure`. Rebuilt Android debug APK afterward to verify the shared native header change.
- [x] **2026-04-26 — Verified core scaffold with direct compiler fallback.** `cmake` is unavailable in the current environment, but `g++ -std=c++20` builds the `time`, `command_stack`, and `ffi` test binaries cleanly with the repo warning flags and `-Werror`; all three binaries pass.
- [x] **2026-04-26 — Added Android Gradle/Compose/JNI scaffold.** Added Gradle wrapper, app module, empty Compose screen, `NativeBridge.nativeVersion()`, and JNI binding to the C ABI `vx_version()` for the Android-first shell. Build verification is blocked only by missing local Android SDK configuration.
- [x] **2026-04-26 — Verified Android debug APK build.** Added ignored `android/local.properties` for `/home/bogdan/Android/Sdk`, enabled AndroidX, pinned `core-ktx` to API-34-compatible `1.13.1`, fixed the native CMake path, and confirmed `cd android && ./gradlew :app:assembleDebug` succeeds.
- [x] **2026-04-26 — Fixed and verified 16 KB page-size emulator compatibility.** Added `-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON` for NDK r27 native linking, rebuilt the APK, verified `libvideoeditor.so` and packaged native libraries use 16 KB `LOAD` segment alignment, verified APK zip alignment with `zipalign -c -P 16 -v 4`, and installed/launched successfully on the Pixel 10 Pro XL emulator.
- [x] **2026-04-26 — Verified Android runtime JNI smoke test.** Installed `app-debug.apk` on `emulator-5554`, launched `com.videoeditor.app/.MainActivity`, confirmed the process stays running, and confirmed the UI hierarchy contains `Video Editor` and `Engine 0.1.0`.
- [x] **2026-04-26 — Implemented Android `VulkanBackend` skeleton.** Added `core/src/platform/vulkan/` with Vulkan instance/device/queue/command-pool setup, image-backed texture allocation/release, no-op command-buffer submission through `dispatchCompute("noop")`, Android CMake linkage to `libvulkan`, and a JNI smoke diagnostic surfaced in the Compose shell.
- [x] **2026-04-26 — Added x86_64 debug native build for emulator parity.** Debug APK now packages `arm64-v8a` and `x86_64`, so x86_64 emulators no longer run the app's native core through ARM translation. Reverified `assembleDebug`, install, APK 16 KB zip alignment, and ELF `LOAD` segment alignment for both `libvideoeditor.so` ABIs.
- [x] **2026-04-26 — Verified first-frame H.264 hardware decode via MediaCodec.** Added a tiny deterministic H.264 MP4 fixture, `MediaCodecFirstFrameDecoder`, and a `TextureView`-backed Compose preview. Installed and launched on the Pixel 10 Pro XL emulator; UI and screenshot confirm a decoded 320x180 `video/avc` frame rendered through `c2.goldfish.h264.decoder`. No timeline or playback loop yet.
- [x] **2026-04-26 — Implemented project model JSON schema v1 round-trip.** Added deterministic serialization/loading for id, name, timestamps, canvas, framerate, and color space; load → save is byte-identical for schema v1 output, and FFI project loading now returns real project handles.
- [x] **2026-04-26 — Added schema migration framework stub.** Added `migrateProjectJsonToCurrent()` so future schema upgrades have a stable entry point; Phase 0 accepts current schema and rejects unsupported future schemas clearly.
- [x] **2026-04-26 — Added `MockGpuBackend` and pixel comparison harness.** Added CPU-backed texture allocation/release, texture copy validation, mock texture byte accessors, and an SSIM helper for off-device render tests. Compute shader emulation intentionally returns `VX_ERR_UNSUPPORTED` until render graph nodes define shader semantics.
- [x] **2026-04-26 — Implemented `vx::Time` and `vx::Rational` skeleton.** Existing code provides rational frame conversion and timebase rebasing, with focused tests added in `core/tests/time_test.cpp`. See [ADR-0005](../adr/0005-rational-time.md).
- [x] **2026-04-26 — Implemented `vx::CommandStack` skeleton.** Existing code provides execute/undo/redo, bounded depth, redo clearing, and focused tests in `core/tests/command_stack_test.cpp`. See [ADR-0008](../adr/0008-command-pattern-undo.md).
- [x] **2026-04-26 — Added GPU and codec abstraction headers.** Existing `IGpuBackend` and `ICodecBackend` interfaces keep the core Android-first but iOS-ready per [ADR-0003](../adr/0003-android-first-then-ios.md) and [ADR-0004](../adr/0004-gpu-backend-android.md).